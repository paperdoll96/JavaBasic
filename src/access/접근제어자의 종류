접근 제어자의 종류
- private : 모든 외부 호출을 막는다
- default : 같은 패키지안에서 호출은 허용한다.
- prodected : 같은 패키지안에서 호출은 허용한다. 패키지가 달라도 상속관계의 호출은 허용
- public : 모든 외부 호출을 허용

private -> default -> protected -> public 순서로 많이 허용!

접근 제어자 사용 위치
- 접근 제어자는 필드와 메서드, 생성자에 사용된다.

클래스 레벨에도 일부 접근 제어자를 사용 할 수 있음!
- 클래스 레벨의 접근 제어자 규칙
 - 클래스 레벨의 접근 제어자는 public, default 만 사용 가능!
 - public 클래스는 반드시 파일명과 이름이 같아야 한다!
        하나의 자바 파일에 public 클래스는 하나만 등장!
                        default 접근 제어자를 사용하는 클래스는 무한정!